/* CREATE TABLE burgers (
    id INT NOT NULL AUTO_INCREMENT,
    name VARCHAR(255),
    cost DECIMAL,
    quantity INT,
    modifications VARCHAR(255),
    itemType ENUM,

    PRIMARY KEY (id)
); */

/* CREATE TABLE drinks (
    id INT NOT NULL AUTO_INCREMENT,
    name VARCHAR(255),
    cost DECIMAL,
    quantity INT,
    modifications VARCHAR(255),
    itemType ENUM,

    PRIMARY KEY (id),
); */

/* CREATE TABLE sides (
    id INT NOT NULL AUTO_INCREMENT,
    name VARCHAR(255),
    cost DECIMAL,
    quantity INT,
    modifications VARCHAR(255),
    itemType ENUM,

    PRIMARY KEY (id),
); */